import { writing } from "../../content";
export const dynamic = "force-static";
export function GET() { const base = "https://mokeshavarz.github.io"; const items = writing.filter((item) => item.type === "Learning Note").map((item) => `<item><title>${item.title}</title><link>${base}/writing/${item.slug}/</link><guid>${base}/writing/${item.slug}/</guid><description>${item.description}</description><pubDate>${new Date(item.date).toUTCString()}</pubDate></item>`).join(""); return new Response(`<?xml version="1.0"?><rss version="2.0"><channel><title>Ario Keshavarz · Learning Notes</title><link>${base}/writing/</link><description>Evolving explanations based on my current understanding.</description>${items}</channel></rss>`, { headers: { "Content-Type": "application/rss+xml" } }); }
