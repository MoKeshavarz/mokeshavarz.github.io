import { writing } from "../../content";
export const dynamic = "force-static";
export function GET() { const base = "https://mokeshavarz.github.io"; const items = writing.filter((item) => item.type === "Article" || item.type === "Case Study").map((item) => `<item><title>${item.title}</title><link>${base}/writing/${item.slug}/</link><guid>${base}/writing/${item.slug}/</guid><description>${item.description}</description><pubDate>${new Date(item.date).toUTCString()}</pubDate></item>`).join(""); return new Response(`<?xml version="1.0"?><rss version="2.0"><channel><title>Mohamad Keshavarz · Articles</title><link>${base}/writing/</link><description>Technical articles and engineering case studies.</description>${items}</channel></rss>`, { headers: { "Content-Type": "application/rss+xml" } }); }
