import type { Metadata } from "next";
import { LibraryExplorer } from "../client";
import { books } from "../content";
import { Footer, Header, PageIntro } from "../components";

export const metadata: Metadata = { title: "Library", description: "A personal knowledge library connecting technical and non-technical books to projects, writing, and practice." };
export default function LibraryPage() { const reading = books.filter((book) => book.readingStatus === "Reading").length; const finished = books.filter((book) => book.readingStatus === "Finished").length; const technical = books.filter((book) => book.bookType === "Technical").length; return <><Header /><main id="main"><PageIntro label="Library / Reading" title="A personal library of ideas in motion." text="Not a bookstore and not a scoreboard—just a record of changing interests, personal interpretations, and the connections between reading and building." /><section className="library-overview"><div className="container"><div><span>Currently reading</span><strong>{reading}</strong><p>Progress is recorded manually.</p></div><div><span>Finished</span><strong>{finished}</strong><p>Summaries are still being documented.</p></div><div><span>Technical titles</span><strong>{technical}</strong><p>Includes one marked placeholder.</p></div><div><span>Most explored</span><strong>Fiction</strong><p>Based on the current shelf.</p></div></div></section><section className="page-section container"><LibraryExplorer items={books} /></section></main><Footer /></>; }
