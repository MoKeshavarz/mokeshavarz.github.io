import { writing } from "../content";
export const dynamic = "force-static";
function escapeXml(value: string) { return value.replace(/[<>&'\"]/g, (char) => ({ "<": "&lt;", ">": "&gt;", "&": "&amp;", "'": "&apos;", '"': "&quot;" })[char] || char); }
export function GET() { const base = "https://mokeshavarz.github.io"; const items = writing.map((item) => `<item><title>${escapeXml(item.title)}</title><link>${base}/writing/${item.slug}/</link><guid>${base}/writing/${item.slug}/</guid><description>${escapeXml(item.description)}</description><pubDate>${new Date(item.date).toUTCString()}</pubDate><category>${escapeXml(item.type)}</category></item>`).join(""); return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Mohamad Keshavarz · Connected Record</title><link>${base}</link><description>Articles, case studies, learning notes, and book summaries.</description>${items}</channel></rss>`, { headers: { "Content-Type": "application/rss+xml; charset=utf-8" } }); }
